# Cassandra Single Node powered by Docker Compose


## Requirements:
* docker >= 17.12.0+
* docker-compose

## Quick Start
* Clone or download this repository
* Go inside of directory,  `cd compose-cassandra`

* Run this command `docker-compose up -d` for start up the node